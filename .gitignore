node_modules
dist
.tmp
.sass-cache
app/bower_components
npm-debug.log
uploads
.env
.envprod
app/projects/private/*
app/projects/public/myExperiment/*
# package-lock.json is intentionally committed: tagged builds are what gets deployed, so the
# dependency tree has to be reproducible. Do not re-ignore it.
